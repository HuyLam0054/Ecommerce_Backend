import express from "express";

// const ctrl_user = require("../controllers/user");
// const { verifyAccessToken, isAdmin } = require("../middlewares/verifyToken");

const router = express.Router();

// router.post("/register", ctrl_user.register);
// router.post("/login", ctrl_user.login);
// router.get("/current", verifyAccessToken, ctrl_user.getCurrent);
// router.post("/refreshtoken", ctrl_user.refreshAccessToken);
// router.get("/logout", ctrl_user.logout);
// router.get("/forgotpassword", ctrl_user.forgotPassword);
// router.put("/resetpassword", ctrl_user.resetPassword);
// router.get("/", [verifyAccessToken, isAdmin], ctrl_user.getUsers);
// router.delete("/", [verifyAccessToken, isAdmin], ctrl_user.deleteUser);
// router.put("/current", [verifyAccessToken], ctrl_user.updateUser);
// router.put("/:uid", [verifyAccessToken, isAdmin], ctrl_user.updateUserByAdmin);

export default router;

// CRUD | Create - Read - Update - Delete | POST - GET - PUT - DELETEeee
// CREATE (POST) + PUT - body
// GET + DELETE - query // ?fdfdsf&fdfs
